#!/bin/bash -e

echo -n "Building with "
go version

echo $GOPATH
export GOPATH=$(pwd)/go
cd $GOPATH/src/github.com/littlebrownham/supermarket

BIN=./build/supermarket
rm -rfv $BIN

CGO_ENABLED=0 GOOS=linux go build -a -installsuffix cgo -o $BIN .

