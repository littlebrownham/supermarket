#!/bin/bash -e

echo -n "Testing with "
go version

export GOPATH=$(pwd)/go
export PATH=$GOPATH/bin:$PATH

echo $GOPATH
echo $PATH

go get -u github.com/golang/dep/cmd/dep
dep ensure

go test -v ./...
